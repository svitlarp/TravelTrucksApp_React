import css from './CamperCatalog.module.css';

const CamperCatalog = () => {
    return (
        <div className={css.camperCatalog}>
            <h4>CamperCatalog</h4>
        </div>
    );
}

export default CamperCatalog;